# Current Architecture Docs

This folder is for current architecture and protocol references that describe the shipped product behavior.

- `board-state-machine.md` — source of truth for lifecycle and board-state invariants.
- `realtime-protocol-phase1.md` — current backend/frontend protocol reference.

Historical rewrite roadmaps were moved to `docs/history/`.
